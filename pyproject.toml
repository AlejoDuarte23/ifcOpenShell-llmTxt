[project]
name = "ifcopenshell-llmtxt"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=2.14.1",
    "openai-agents>=0.6.4",
    "pytest>=9.0.2",
]
